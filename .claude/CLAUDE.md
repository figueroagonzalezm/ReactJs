# Project Instructions

## Git Commits

When the user asks to commit changes, ALWAYS use the `/commit` skill instead of running `git commit` commands directly.

The commit skill is located at `.claude/skills/commit/` and provides structured commit message creation.
